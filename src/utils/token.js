/**
 * Replace this with an Personal Access API token. It needs the following permissions:
 *  public_repo,
 *  read:discussion,
 *  read:org,
 *  read:repo_hook,
 *  read:user,
 *  repo:status,
 *  repo_deployment,
 *  user:email,
 *  user:follow
 */
export const GITHUB_TOKEN = "";
